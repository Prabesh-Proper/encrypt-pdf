# encrypt-pdf
CryptoPoweredPDF is a Flask-based web app that encrypts and decrypts PDF files using AES-256. It uses PBKDF2 with 100,000 iterations, random salt, and IV for secure key generation. All processing happens in memory with no file storage, providing strong protection against unauthorized access and file interception.
